# Copy to `.env`. `.env` is gitignored and never leaves this machine.

# Fixed so the Even App's saved Terminal Mode pairing stays valid across
# restarts, instead of a new random token/port invalidating it each time.
TERMINAL_PORT=3456
TERMINAL_TOKEN=
